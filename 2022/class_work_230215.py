
import re
from pprint import pprint
# def move_zeros(lst):
#     new_list = [item for item in lst if item != 0 in lst]
#     new_list += [0] * lst.count(0)
#     return print(new_list)


# move_zeros([1, 0, 0, 7, 5, 0, 8])

def big_list(l):
    return [i for i in range(0, l+1)]


print(big_list(100))
